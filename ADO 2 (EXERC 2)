import java.util.*;
class Main {
  public static void main(String[] args) {
    int n;
    Scanner scan = new Scanner(System.in);

    System.out.println("Digite o valor de N");
    n = scan.nextInt();

    System.out.println("3 primeiros múltiplos de " + n + ": " + n + ", " + (2 * n ) + "e " + (3 * n));
  }
}
